<h1>WELCOME TO WOLF NEWZ</h1>

<%= button_to "login", new_login_path, method: :get %><br>
<%= button_to "register", new_user_path, method: :get %>


<h3>Sample Articles:</h3>
<ul>
  <% samples = @articles.sample(3) %>
  <% samples.each do |article| %>
  <li>
    <%= link_to article.title, article_path(article) %>
  </li>
  <% end %>
</ul>
